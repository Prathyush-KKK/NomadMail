{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nomadinbox.local/schemas/message.v1.json",
  "title": "NomadInbox Message",
  "type": "object",
  "required": ["id", "provider", "providerMessageId", "subject", "from", "receivedAt"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "id": { "type": "string" },
    "accountId": { "type": ["string", "null"] },
    "provider": { "type": "string", "enum": ["gmail-api", "outlook-graph", "outlook-desktop", "archive-import", "sample"] },
    "providerMessageId": { "type": "string" },
    "conversationId": { "type": ["string", "null"] },
    "threadKey": { "type": ["string", "null"] },
    "folder": { "type": ["string", "null"] },
    "subject": { "type": "string" },
    "from": { "$ref": "#/$defs/address" },
    "to": { "type": "array", "items": { "$ref": "#/$defs/address" } },
    "cc": { "type": "array", "items": { "$ref": "#/$defs/address" } },
    "receivedAt": { "type": "string", "format": "date-time", "description": "UTC ISO 8601 timestamp. Ambiguous source times are parsed with the user's locale and time zone before storage." },
    "sentAt": { "type": ["string", "null"], "description": "UTC ISO 8601 timestamp when available." },
    "snippet": { "type": ["string", "null"] },
    "bodyText": { "type": ["string", "null"] },
    "bodyHtml": { "type": ["string", "null"] },
    "bodyTextAvailable": { "type": "boolean" },
    "bodyHtmlAvailable": { "type": "boolean" },
    "headers": { "type": "object", "additionalProperties": { "type": "string" } },
    "unread": { "type": "boolean" },
    "flagged": { "type": "boolean" },
    "importance": { "type": ["string", "null"] },
    "categories": { "type": "array", "items": { "type": "string" } },
    "attachments": { "type": "array", "items": { "$ref": "#/$defs/attachment" } },
    "capabilities": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["openInClient", "reply", "replyAll", "forward", "markRead", "markUnread", "flag", "unflag", "move", "archive", "trash", "saveAttachment"]
      }
    },
    "sourceType": { "type": ["string", "null"], "enum": ["live-sync", "archive-import", null] },
    "sourceProvider": { "type": ["string", "null"] },
    "sourcePathHash": { "type": ["string", "null"] },
    "importBatchId": { "type": ["string", "null"] },
    "providerRawRef": { "type": ["string", "null"] },
    "rawCaptured": { "type": "boolean" },
    "extractionStatus": { "type": ["string", "null"], "enum": ["notStarted", "pending", "complete", "failed", null] },
    "normalizedAt": { "type": ["string", "null"], "format": "date-time" },
    "actionable": { "type": "boolean", "default": true },
    "importedAt": { "type": ["string", "null"], "format": "date-time", "description": "UTC ISO 8601 import timestamp." }
  },
  "$defs": {
    "address": {
      "type": "object",
      "required": ["email"],
      "properties": {
        "name": { "type": ["string", "null"] },
        "email": { "type": "string" }
      }
    },
    "attachment": {
      "type": "object",
      "required": ["id", "name"],
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "contentType": { "type": ["string", "null"] },
        "sizeBytes": { "type": ["integer", "null"] },
        "localPath": { "type": ["string", "null"] }
      }
    }
  }
}
